# BDC 2.3.3-dev463

## Direct old-backup deletion

- Replaces the hidden typed-phrase delete panel with a visible Delete button on every backup row.
- Keeps a browser confirmation before each permanent deletion.
- Allows multiple old backups to be selected and deleted together with one confirmation.
- Blocks bulk deletion when no backup is selected.
- Retains Super Admin authorization, CSRF protection and strict server-side backup filename validation.

No scoring, competition, competitor, judge, point, result, projection or backup-creation behavior is changed.
