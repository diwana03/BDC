<?php
declare(strict_types=1);require dirname(__DIR__,2).'/bootstrap.php';use App\Core\Auth;use App\Core\Database;Auth::requirePermission('registrations.manage');$s=Database::connection()->prepare('SELECT * FROM bdc_registration_uploads WHERE id=:id');$s->execute(['id'=>(int)($_GET['id']??0)]);$f=$s->fetch();if(!$f){http_response_code(404);exit('File not found.');}$path=dirname(__DIR__,2).'/'.$f['storage_path'];if(!is_file($path)){http_response_code(404);exit('Stored file missing.');}header('Content-Type: '.$f['mime_type']);header('Content-Length: '.filesize($path));header('Content-Disposition: inline; filename="'.basename($f['original_filename']).'"');readfile($path);
