<?php
declare(strict_types=1);
require dirname(__DIR__,2).'/bootstrap.php';
use App\Core\Auth;use App\Core\Database;
Auth::requireAdmin();$roundId=(int)($_GET['round_id']??0);$pdo=Database::connection();$s=$pdo->prepare("SELECT dance_style,division FROM bdc_scoring_rounds WHERE id=:id AND round_type='final' LIMIT 1");$s->execute(['id'=>$roundId]);$round=$s->fetch();if(!$round){http_response_code(404);exit('Final round not found.');}if((string)$round['dance_style']!=='salsa'){header('Location: publish.php?round_id='.$roundId,true,303);exit;}?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Salsa Publication Safety Check | BDC</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"></head><body class="bg-light"><main class="container py-5" style="max-width:760px"><div class="card border-warning shadow-sm"><div class="card-body p-4"><div class="text-warning fw-bold text-uppercase small">Publication Safety Lock</div><h1 class="h3 mt-2">Salsa results are not being published yet</h1><p>The scoring round is safely stored as Salsa, but publication is temporarily blocked until the point transaction and result publication path has been verified to write only to the Salsa ledger.</p><p class="mb-4"><strong>No Bachata points have been written.</strong></p><a class="btn btn-dark" href="index.php?round_id=<?=$roundId?>">Back to Scoring Dashboard</a></div></div></main></body></html>