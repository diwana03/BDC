# BDC 2.3.3-dev12

## Participant Results

- Corrects the PHP syntax regression introduced in `2.3.3-dev11` that caused every Participant Results request to return HTTP 500.
- Restores Participant Results rendering on desktop and mobile.
- Preserves the Aggregate Points summary, participant history, filters and existing point-ledger calculation unchanged.

## Safety

No database schema, scoring formula, leaderboard calculation, repository document, publishing workflow or page layout changed.
