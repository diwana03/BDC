# Release 2.3.3-dev603

## Competitor projector display enhancement

- Gives every split-role competitor card one consistent premium navy background.
- Places every photo inside the same neutral circular frame without modifying the original image.
- Separates photo, name, prominent bib, flag and country into dedicated rows.
- Prevents names, flags and country text from overlapping.
- Ellipsizes unusually long country/location values instead of wrapping across cards.
- Uses container-responsive sizing so dense Leader and Follower grids remain inside their panels.
- Preserves balanced pagination, bib order, role separation and Test/Live parity.
