# Release 2.3.3-dev606

## Non-collapsing projector identity cards

- Replaces the failed percentage/container-based competitor portrait sizing with a fixed two-column card.
- Keeps every competitor portrait visible at a stable projector-safe size.
- Places the photo on the left and name, prominent bib, flag and country on the right.
- Removes the layout dependency that caused portraits to collapse into dots.
- Gives judge photo frames an explicit reserved height so portraits cannot cover names.
- Preserves uniform backgrounds, balanced pagination, role separation, scoring data and Test/Live parity.
