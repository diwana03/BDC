# BDC 2.3.3-dev178

## Release Manager access

- Keeps Release Manager as a permanent top-level sidebar item.
- Adds a DEPLOY badge so it is immediately recognizable.

## Production navigation theme

- Uses white navigation folders with Unisphere blue headers and icons.
- Uses dark readable links with blue hover and active states.
- Keeps the separate amber high-contrast Staging theme.
- Bumps the stylesheet cache version.

## Deployment

- No database migration is required.
- Targets Staging through the `develop` branch for validation first.
