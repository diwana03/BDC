# BDC 2.3.3-dev559

- Matches profile submissions directly by SDC ID.
- Allows an existing SDC-only profile with an unknown shared Bachata role to match safely by its unique identity fields.
- Repairs the two pending SDC photo proposals for Sasa and So Young so they target the existing SDC profiles instead of creating duplicates.
