# BDC 2.3.3-dev597

## Automatic Judge selection correction

- Shows each active judge exactly once.
- Keeps name, Judge ID and country searchable within that one result.
- Stops rebuilding the suggestion list while the administrator is typing.
- Allows a suggestion to be selected normally.
- Resolves the selected result to the canonical judge name and directory ID.
